function List (users) {
  console.log(users);

  return (
    <>
    </>
  // <ul>
  //     {user.user.map( item =><li key={item.passport}>{item.passport}{item.name}{item.age} </li> )}
  // </ul>
  );
}


export default List;